import { ScrollView, Text } from 'react-native';
import React from 'react';

const Match = () => {
  return (
    <ScrollView>
      <Text>Jopa</Text>
    </ScrollView>
  );
};

export default Match;
