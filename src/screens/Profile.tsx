import { ScrollView, Text } from 'react-native';
import React from 'react';

const Profile = () => {
  return (
    <ScrollView>
      <Text>Profile</Text>
    </ScrollView>
  );
};

export default Profile;
